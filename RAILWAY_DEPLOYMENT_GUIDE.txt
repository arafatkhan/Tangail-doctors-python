╔══════════════════════════════════════════════════════════════════════════════╗
║                 RAILWAY.COM DEPLOYMENT SETUP GUIDE                           ║
║              Tangail Doctors Python - Django Project                         ║
║                     Multi-Language Support Enabled                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

📋 PROJECT OVERVIEW:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Project Name: Tangail Doctors List
Type: Django 4.2 Web Application
Database: SQLite3 (Development) → PostgreSQL (Production)
Language Support: Bangla (bn) & English (en)
Features: Doctor listing, Emergency services, Appointments, Multi-language


═══════════════════════════════════════════════════════════════════════════════
                        STEP 1: PRE-DEPLOYMENT CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

✅ Files Already Present:
  • requirements.txt (needs updates)
  • manage.py
  • config/ (settings, urls, wsgi)
  • doctors/ (main app)
  • locale/ (translation files)
  • media/ (user uploads)
  • db.sqlite3 (development database)

⚠️ Files to Create (Required for Railway):
  1. runtime.txt           - Python version specification
  2. railway.json          - Railway configuration
  3. Procfile              - Process commands
  4. nixpacks.toml         - Build configuration
  5. .railwayignore        - Exclude files from deployment
  6. staticfiles/          - Collected static files directory


═══════════════════════════════════════════════════════════════════════════════
                     STEP 2: REQUIRED FILES TO CREATE
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ FILE 1: runtime.txt                                                         │
│ Purpose: Specify Python version for Railway                                │
└─────────────────────────────────────────────────────────────────────────────┘

Content:
python-3.11.0


┌─────────────────────────────────────────────────────────────────────────────┐
│ FILE 2: Procfile                                                            │
│ Purpose: Define how Railway should run your app                            │
└─────────────────────────────────────────────────────────────────────────────┘

Content:
web: gunicorn config.wsgi --log-file -
release: python manage.py migrate && python compile_translations.py && python manage.py collectstatic --noinput


┌─────────────────────────────────────────────────────────────────────────────┐
│ FILE 3: railway.json                                                        │
│ Purpose: Railway-specific configuration                                     │
└─────────────────────────────────────────────────────────────────────────────┘

Content:
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python compile_translations.py"
  },
  "deploy": {
    "startCommand": "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}


┌─────────────────────────────────────────────────────────────────────────────┐
│ FILE 4: nixpacks.toml                                                       │
│ Purpose: Nixpacks build configuration                                       │
└─────────────────────────────────────────────────────────────────────────────┘

Content:
[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
    "pip install gunicorn psycopg2-binary whitenoise python-decouple dj-database-url"
]

[phases.build]
cmds = [
    "python compile_translations.py",
    "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120"


┌─────────────────────────────────────────────────────────────────────────────┐
│ FILE 5: .railwayignore                                                      │
│ Purpose: Exclude unnecessary files from deployment                          │
└─────────────────────────────────────────────────────────────────────────────┘

Content:
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
venv/
env/

# Django
db.sqlite3
db.sqlite3-journal
*.log
local_settings.py

# Development files
.git
.gitignore
.vscode/
.idea/
*.md
PHASE_2_BRANCH_DECISION.txt
FEATURES_REVIEW.txt
TEST_CALCULATOR_PLAN.pdf

# Media files (optional - remove if you want to deploy media)
# media/

# Documentation
MULTI_LANGUAGE_IMPLEMENTATION_PLAN.md


═══════════════════════════════════════════════════════════════════════════════
               STEP 3: UPDATE requirements.txt (CRITICAL!)
═══════════════════════════════════════════════════════════════════════════════

Your current requirements.txt is INCOMPLETE for production!

┌─────────────────────────────────────────────────────────────────────────────┐
│ UPDATED requirements.txt (Complete Production-Ready)                        │
└─────────────────────────────────────────────────────────────────────────────┘

# Django Core
Django==4.2.7
asgiref==3.8.1
sqlparse==0.5.5
tzdata==2025.3

# Production Server
gunicorn==22.0.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Static Files
whitenoise==6.6.0

# Image Processing
Pillow==10.3.0

# Environment Variables
python-decouple==3.8

# Translation Support
polib==1.2.0

# Optional - Google Translate API (for auto-translate feature)
# googletrans==4.0.0rc1


═══════════════════════════════════════════════════════════════════════════════
            STEP 4: UPDATE config/settings.py (PRODUCTION SETTINGS)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Changes Required in settings.py                                             │
└─────────────────────────────────────────────────────────────────────────────┘

1. ADD AT THE TOP (after imports):
   ───────────────────────────────────────────────────────────────────────────
   from decouple import config, Csv
   import dj_database_url


2. REPLACE SECRET_KEY:
   ───────────────────────────────────────────────────────────────────────────
   # OLD:
   SECRET_KEY = 'django-insecure-g#qbaiiv=@jyaht9q7k5d0gb)9ri2n!60wx(av!*4ppb@c!qqo'

   # NEW:
   SECRET_KEY = config('SECRET_KEY', default='django-insecure-g#qbaiiv=@jyaht9q7k5d0gb)9ri2n!60wx(av!*4ppb@c!qqo')


3. UPDATE DEBUG:
   ───────────────────────────────────────────────────────────────────────────
   # OLD:
   DEBUG = True

   # NEW:
   DEBUG = config('DEBUG', default=False, cast=bool)


4. UPDATE ALLOWED_HOSTS:
   ───────────────────────────────────────────────────────────────────────────
   # OLD:
   ALLOWED_HOSTS = []

   # NEW:
   ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='localhost,127.0.0.1', cast=Csv())


5. UPDATE DATABASES (CRITICAL - PostgreSQL for Production):
   ───────────────────────────────────────────────────────────────────────────
   # REPLACE entire DATABASES section with:

   DATABASES = {
       'default': dj_database_url.config(
           default=config('DATABASE_URL', default=f'sqlite:///{BASE_DIR / "db.sqlite3"}'),
           conn_max_age=600,
           conn_health_checks=True,
       )
   }


6. ADD MIDDLEWARE (after 'django.middleware.security.SecurityMiddleware'):
   ───────────────────────────────────────────────────────────────────────────
   MIDDLEWARE = [
       'django.middleware.security.SecurityMiddleware',
       'whitenoise.middleware.WhiteNoiseMiddleware',  # ADD THIS LINE
       'django.contrib.sessions.middleware.SessionMiddleware',
       # ... rest of middleware
   ]


7. UPDATE STATIC FILES SETTINGS (Replace entire section):
   ───────────────────────────────────────────────────────────────────────────
   # Static files (CSS, JavaScript, Images)
   STATIC_URL = '/static/'
   STATIC_ROOT = BASE_DIR / 'staticfiles'
   STATICFILES_DIRS = [BASE_DIR / 'doctors' / 'static']

   # WhiteNoise configuration
   STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

   # Media files
   MEDIA_URL = '/media/'
   MEDIA_ROOT = BASE_DIR / 'media'


8. ADD SECURITY SETTINGS (at the end of file):
   ───────────────────────────────────────────────────────────────────────────
   # Security Settings (Production)
   if not DEBUG:
       SECURE_SSL_REDIRECT = True
       SESSION_COOKIE_SECURE = True
       CSRF_COOKIE_SECURE = True
       SECURE_BROWSER_XSS_FILTER = True
       SECURE_CONTENT_TYPE_NOSNIFF = True
       X_FRAME_OPTIONS = 'DENY'
       SECURE_HSTS_SECONDS = 31536000
       SECURE_HSTS_INCLUDE_SUBDOMAINS = True
       SECURE_HSTS_PRELOAD = True


═══════════════════════════════════════════════════════════════════════════════
                  STEP 5: CREATE .env FILE (FOR RAILWAY)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ .env (Local Development - DO NOT commit to Git!)                           │
└─────────────────────────────────────────────────────────────────────────────┘

DEBUG=True
SECRET_KEY=django-insecure-g#qbaiiv=@jyaht9q7k5d0gb)9ri2n!60wx(av!*4ppb@c!qqo
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3


┌─────────────────────────────────────────────────────────────────────────────┐
│ Railway Environment Variables (Set in Railway Dashboard)                    │
└─────────────────────────────────────────────────────────────────────────────┘

You will set these in Railway dashboard:

1. DEBUG=False
2. SECRET_KEY=<generate-new-random-key>
3. ALLOWED_HOSTS=your-app-name.railway.app
4. DATABASE_URL=<Railway will auto-generate this>


═══════════════════════════════════════════════════════════════════════════════
                    STEP 6: UPDATE .gitignore (IMPORTANT!)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Add to .gitignore (if not present)                                          │
└─────────────────────────────────────────────────────────────────────────────┘

# Environment variables
.env
.env.local
.env.production

# Static files
staticfiles/
static_collected/

# Media files (optional)
# media/

# Database
db.sqlite3
db.sqlite3-journal


═══════════════════════════════════════════════════════════════════════════════
                 STEP 7: RAILWAY DEPLOYMENT PROCESS
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ A. Initial Setup on Railway.com                                             │
└─────────────────────────────────────────────────────────────────────────────┘

1. Go to https://railway.app/
2. Sign up/Login (GitHub recommended)
3. Click "New Project"
4. Select "Deploy from GitHub repo"
5. Choose: arafatkhan/Tangail-doctors-python
6. Railway will auto-detect Django project


┌─────────────────────────────────────────────────────────────────────────────┐
│ B. Add PostgreSQL Database (CRITICAL!)                                      │
└─────────────────────────────────────────────────────────────────────────────┘

1. In your Railway project, click "+ New"
2. Select "Database" → "PostgreSQL"
3. Railway will create a database and set DATABASE_URL automatically
4. Link it to your Django service


┌─────────────────────────────────────────────────────────────────────────────┐
│ C. Configure Environment Variables                                          │
└─────────────────────────────────────────────────────────────────────────────┘

Go to Project → Variables → Add these:

Required Variables:
┌──────────────────────┬─────────────────────────────────────────────────────┐
│ Variable Name        │ Value                                               │
├──────────────────────┼─────────────────────────────────────────────────────┤
│ DEBUG                │ False                                               │
│ SECRET_KEY           │ [Generate new key - see below]                      │
│ ALLOWED_HOSTS        │ your-app-name.railway.app,$RAILWAY_PUBLIC_DOMAIN    │
│ DJANGO_SETTINGS...   │ config.settings                                     │
│ PORT                 │ ${{PORT}} (Railway auto-sets)                       │
│ DATABASE_URL         │ (Auto-set by PostgreSQL service)                    │
└──────────────────────┴─────────────────────────────────────────────────────┘

Generate SECRET_KEY:
Run in Python:
>>> from django.core.management.utils import get_random_secret_key
>>> print(get_random_secret_key())


┌─────────────────────────────────────────────────────────────────────────────┐
│ D. Deploy Settings                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

1. Railway Settings → General:
   • Service Name: tangail-doctors-web
   • Region: Choose closest to Bangladesh (Asia Pacific)
   
2. Railway Settings → Networking:
   • Enable Public Networking
   • Generate Domain (or use custom domain)

3. Railway Settings → Build:
   • Build Command: (auto-detected from railway.json)
   • Start Command: (auto-detected from Procfile)


┌─────────────────────────────────────────────────────────────────────────────┐
│ E. Manual Deployment Steps (First Time)                                     │
└─────────────────────────────────────────────────────────────────────────────┘

After Railway auto-deploys, you may need to run:

Option 1: Via Railway CLI
$ railway login
$ railway link
$ railway run python manage.py migrate
$ railway run python manage.py createsuperuser
$ railway run python compile_translations.py

Option 2: Via Railway Dashboard
1. Go to Deployments → Latest Deployment
2. Click "View Logs"
3. Check if migrations ran successfully
4. Create superuser via Django admin shell


═══════════════════════════════════════════════════════════════════════════════
                    STEP 8: POST-DEPLOYMENT CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

✅ After Successful Deployment:

1. Access your site: https://your-app.railway.app
2. Test homepage loads (should show Bangla by default)
3. Test language switcher (Bangla ↔ English)
4. Access admin panel: https://your-app.railway.app/admin/
5. Create superuser if needed
6. Upload doctor images via admin
7. Test all features:
   ✓ Doctor listing
   ✓ Search functionality
   ✓ Emergency doctors page
   ✓ Hospital list
   ✓ Category filtering
   ✓ Language switching
   ✓ Mobile responsiveness


═══════════════════════════════════════════════════════════════════════════════
                       STEP 9: TROUBLESHOOTING GUIDE
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Common Issues & Solutions                                                   │
└─────────────────────────────────────────────────────────────────────────────┘

❌ ISSUE 1: "DisallowedHost at /"
   ─────────────────────────────────────────────────────────────────────────
   Solution: Add Railway domain to ALLOWED_HOSTS in environment variables:
   ALLOWED_HOSTS=your-app.railway.app,$RAILWAY_PUBLIC_DOMAIN


❌ ISSUE 2: Static files not loading (CSS/JS missing)
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   1. Check STATIC_ROOT is set correctly
   2. Run: railway run python manage.py collectstatic
   3. Verify WhiteNoise is in MIDDLEWARE
   4. Check staticfiles/ directory exists


❌ ISSUE 3: Database connection errors
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   1. Verify PostgreSQL service is linked
   2. Check DATABASE_URL variable is set
   3. Ensure psycopg2-binary is in requirements.txt
   4. Run migrations: railway run python manage.py migrate


❌ ISSUE 4: Translation not working
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   1. Ensure compile_translations.py runs in build phase
   2. Check locale/ directory exists with .mo files
   3. Verify polib is in requirements.txt
   4. Run: railway run python compile_translations.py


❌ ISSUE 5: Media files (doctor images) not showing
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   For production, use Railway Volumes or external storage (Cloudinary, AWS S3)
   
   Quick fix for Railway:
   1. Go to Railway project settings
   2. Create a Volume: /app/media
   3. Link to your service
   4. Media files will persist across deployments


❌ ISSUE 6: Build failing
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   1. Check requirements.txt has all dependencies
   2. Verify Python version in runtime.txt
   3. Review build logs in Railway dashboard
   4. Ensure no syntax errors in code


❌ ISSUE 7: Application timeout/crashes
   ─────────────────────────────────────────────────────────────────────────
   Solution:
   1. Increase gunicorn timeout in Procfile:
      gunicorn config.wsgi --timeout 120 --workers 3
   2. Check Railway logs for errors
   3. Monitor memory usage (upgrade plan if needed)


═══════════════════════════════════════════════════════════════════════════════
                    STEP 10: ADVANCED CONFIGURATION
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ A. Custom Domain Setup                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

1. Buy domain (e.g., tangaildoctors.com)
2. Railway → Settings → Networking → Custom Domain
3. Add your domain
4. Update DNS records:
   • A Record: Point to Railway IP
   • CNAME: www → your-app.railway.app
5. Update ALLOWED_HOSTS in Railway variables


┌─────────────────────────────────────────────────────────────────────────────┐
│ B. Email Configuration (for appointments, notifications)                    │
└─────────────────────────────────────────────────────────────────────────────┘

Add to Railway environment variables:

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

Then update settings.py to use these variables.


┌─────────────────────────────────────────────────────────────────────────────┐
│ C. Media Files Storage (Recommended for Production)                         │
└─────────────────────────────────────────────────────────────────────────────┘

Option 1: Cloudinary (Free tier available)
   1. Sign up at cloudinary.com
   2. Install: pip install cloudinary django-cloudinary-storage
   3. Configure in settings.py
   4. Doctor images will be stored in cloud

Option 2: Railway Volumes (Persistent storage)
   1. Railway → Project → New → Volume
   2. Mount path: /app/media
   3. Link to Django service
   4. Media persists across deployments


┌─────────────────────────────────────────────────────────────────────────────┐
│ D. Monitoring & Logging                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

1. Railway Logs:
   • Project → Deployments → View Logs
   • Real-time logs of your application
   • Filter by severity (info, warning, error)

2. Django Admin Logs:
   • Enable Django logging in settings.py
   • Monitor user actions, errors, queries

3. Health Checks:
   • Railway auto-monitors your app
   • Configure healthcheckPath in railway.json
   • Get alerts on downtime


═══════════════════════════════════════════════════════════════════════════════
                      STEP 11: CI/CD WORKFLOW (AUTO-DEPLOY)
═══════════════════════════════════════════════════════════════════════════════

✅ Railway Automatically Deploys When:

1. You push to main branch on GitHub
2. Railway detects changes
3. Runs build process
4. Deploys new version
5. Zero-downtime deployment

Manual Control:
• Pause auto-deploy: Railway → Settings → Disable auto-deploy
• Manual deploy: Railway → Deployments → Redeploy


═══════════════════════════════════════════════════════════════════════════════
                       STEP 12: COST & PERFORMANCE
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Railway Pricing (as of 2024)                                                │
└─────────────────────────────────────────────────────────────────────────────┘

Free Plan:
• $5 free credits/month
• Perfect for testing & small projects
• Includes: Web service + PostgreSQL
• Automatic SSL certificates
• Custom domain support

Hobby Plan ($5/month):
• More resources
• Better for production
• No credit limit
• Priority support


┌─────────────────────────────────────────────────────────────────────────────┐
│ Performance Optimization Tips                                               │
└─────────────────────────────────────────────────────────────────────────────┘

1. Enable Database Connection Pooling:
   DATABASES['default']['CONN_MAX_AGE'] = 600

2. Use Django Caching:
   Install redis: railway add redis
   Configure Django cache backend

3. Compress Static Files:
   WhiteNoise already handles this

4. Enable Gzip:
   Already configured in gunicorn

5. Database Indexing:
   Your models already have indexes (good!)


═══════════════════════════════════════════════════════════════════════════════
                     FINAL DEPLOYMENT COMMAND SUMMARY
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Quick Deployment Steps (After all files created)                            │
└─────────────────────────────────────────────────────────────────────────────┘

1. git add .
2. git commit -m "Add Railway deployment configuration"
3. git push origin main
4. Go to railway.app
5. New Project → Deploy from GitHub
6. Select repository
7. Add PostgreSQL database
8. Configure environment variables
9. Wait for deployment (5-10 minutes)
10. Access your site!


═══════════════════════════════════════════════════════════════════════════════
                        EMERGENCY ROLLBACK PROCEDURE
═══════════════════════════════════════════════════════════════════════════════

If deployment fails or breaks:

1. Railway → Deployments → Find working deployment
2. Click "Redeploy" on previous working version
3. Or: git revert to working commit and push


═══════════════════════════════════════════════════════════════════════════════
                            USEFUL LINKS & RESOURCES
═══════════════════════════════════════════════════════════════════════════════

• Railway Dashboard: https://railway.app/dashboard
• Railway Docs: https://docs.railway.com/
• Railway CLI: https://docs.railway.com/develop/cli
• Django Deployment Checklist: https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/
• Railway Community: https://discord.gg/railway


═══════════════════════════════════════════════════════════════════════════════
                                NEXT STEPS
═══════════════════════════════════════════════════════════════════════════════

📋 Your Action Items:

1. ✅ Review this guide thoroughly
2. ✅ Create all required files (I can help you with this)
3. ✅ Update settings.py with production settings
4. ✅ Update requirements.txt with all dependencies
5. ✅ Test locally with DEBUG=False
6. ✅ Commit and push to GitHub
7. ✅ Deploy to Railway
8. ✅ Configure environment variables
9. ✅ Run migrations
10. ✅ Create superuser
11. ✅ Test all features
12. ✅ Add doctor data via admin
13. ✅ Share with users!


═══════════════════════════════════════════════════════════════════════════════

💬 QUESTIONS? আমাকে বলুন:
   • "Create all Railway files" - সব files তৈরি করে দিবো
   • "Update settings.py" - Production settings add করে দিবো
   • "Show me deployment steps" - Step by step guide দিবো
   • "Fix specific error" - Error solve করে দিবো

Ready to deploy? Let me know what you need! 🚀

═══════════════════════════════════════════════════════════════════════════════

Created: February 8, 2026
For: Tangail Doctors Python Project
By: GitHub Copilot
Version: 1.0
