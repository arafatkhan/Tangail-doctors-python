# ইউজার অথেন্টিকেশন - দ্রুত নির্দেশিকা

## 🎯 নতুন ফিচার

### ১. রেজিস্ট্রেশন ও লগইন
- নতুন ইউজার একাউন্ট তৈরি করা যাবে
- ইউজারনেম ও পাসওয়ার্ড দিয়ে লগইন/লগআউট
- নেভিগেশন বারে লগইন স্ট্যাটাস দেখানো

### ২. ফেভারিট ডাক্তার
- যেকোনো ডাক্তারকে ফেভারিটে সেভ করা
- প্রোফাইল থেকে সব ফেভারিট দেখা
- এক ক্লিকে ফেভারিট যোগ/সরানো

### ৩. ডাক্তারের রিভিউ
- ১-৫ স্টার রেটিং দেওয়া
- মতামত লেখা
- নিজের রিভিউ আপডেট করা
- সব রিভিউ দেখা

### ৪. ইউজার প্রোফাইল
- ফেভারিট ডাক্তারদের তালিকা
- রিভিউ করা ডাক্তারদের তালিকা
- দ্রুত অ্যাকশন (দেখুন, সরান)

## 📁 নতুন ফাইল

```
doctors/
├── models.py (আপডেট)              # Favorite ও Review মডেল
├── views.py (আপডেট)               # Authentication ভিউ
├── urls.py (আপডেট)                # নতুন URL
├── admin.py (আপডেট)               # Favorite ও Review অ্যাডমিন
├── templates/doctors/
│   ├── register.html (নতুন)       # রেজিস্ট্রেশন পেজ
│   ├── login.html (নতুন)          # লগইন পেজ
│   ├── profile.html (নতুন)        # প্রোফাইল পেজ
│   ├── detail.html (আপডেট)        # রিভিউ ও ফেভারিট সহ
│   └── base.html (আপডেট)          # লগইন/রেজিস্টার বাটন সহ
└── migrations/
    └── 0004_review_favorite.py (নতুন)
```

## 🔗 নতুন URL

| URL | বিবরণ | লগইন লাগবে? |
|-----|-------|-------------|
| `/register/` | রেজিস্ট্রেশন পেজ | না |
| `/login/` | লগইন পেজ | না |
| `/logout/` | লগআউট | হ্যাঁ |
| `/profile/` | ইউজার প্রোফাইল | হ্যাঁ |
| `/doctor/<id>/favorite/add/` | ফেভারিট যোগ | হ্যাঁ |
| `/doctor/<id>/favorite/remove/` | ফেভারিট সরানো | হ্যাঁ |
| `/doctor/<id>/review/add/` | রিভিউ দেওয়া | হ্যাঁ |

## 🚀 ব্যবহার

### রেজিস্ট্রেশন করুন
1. হোম পেজে যান: http://127.0.0.1:8000/
2. "রেজিস্টার" বাটনে ক্লিক করুন
3. ইউজারনেম ও পাসওয়ার্ড দিন
4. "রেজিস্টার করুন" ক্লিক করুন

### লগইন করুন
1. "লগইন" বাটনে ক্লিক করুন
2. ইউজারনেম ও পাসওয়ার্ড দিন
3. "লগইন করুন" ক্লিক করুন

### ফেভারিট যোগ করুন
1. যেকোনো ডাক্তারের পেজে যান
2. "ফেভারিটে যোগ করুন" ক্লিক করুন

### রিভিউ দিন
1. ডাক্তারের পেজে স্ক্রল করুন
2. রেটিং নির্বাচন করুন (১-৫)
3. মতামত লিখুন
4. "রিভিউ জমা দিন" ক্লিক করুন

### প্রোফাইল দেখুন
1. নেভিগেশন বারে আপনার ইউজারনেমে ক্লিক করুন

## ⚙️ অ্যাডমিন প্যানেল

অ্যাডমিন প্যানেলে এখন নতুন সেকশন:

1. **Favorites** - সব ইউজারের ফেভারিট দেখুন
2. **Reviews** - সব রিভিউ দেখুন ও সম্পাদনা করুন

অ্যাডমিন: http://127.0.0.1:8000/admin/

## 🎨 ডিজাইন

- বাংলা ফন্ট ও ভাষা সাপোর্ট
- Bootstrap 5 রেসপন্সিভ ডিজাইন
- Font Awesome আইকন
- সুন্দর কার্ড ও ফর্ম লেআউট
- সাফল্য/এরর মেসেজ

## 📊 ডাটাবেস

### Favorite টেবিল
- id (Auto)
- user (ForeignKey)
- doctor (ForeignKey)
- created_at (DateTime)

### Review টেবিল
- id (Auto)
- user (ForeignKey)
- doctor (ForeignKey)
- rating (1-5)
- comment (Text)
- created_at (DateTime)
- updated_at (DateTime)

## ✅ সম্পন্ন

- ✅ ইউজার রেজিস্ট্রেশন
- ✅ লগইন/লগআউট
- ✅ ফেভারিট ফিচার
- ✅ রিভিউ সিস্টেম
- ✅ ইউজার প্রোফাইল
- ✅ অ্যাডমিন ইন্টিগ্রেশন
- ✅ বাংলা ভাষা সাপোর্ট
- ✅ রেসপন্সিভ ডিজাইন

সব কিছু কাজ করছে! 🎉
