╔══════════════════════════════════════════════════════════════════════════════╗
║             RAILWAY MCP SERVER SETUP - USER LEVEL CONFIGURATION              ║
║                  Deploy Directly from VS Code / Claude Desktop               ║
╚══════════════════════════════════════════════════════════════════════════════╝

📋 WHAT IS RAILWAY MCP SERVER?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MCP (Model Context Protocol) server allows AI assistants like GitHub Copilot or
Claude to directly interact with Railway.com for deployments, without manual steps.

✅ Benefits:
   • Deploy with simple commands like "deploy to Railway"
   • Check deployment status from chat
   • View logs directly
   • Manage environment variables
   • No need to switch to Railway dashboard


═══════════════════════════════════════════════════════════════════════════════
              OPTION 1: VS CODE (GitHub Copilot) - RECOMMENDED
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Install Railway CLI (Required)                                      │
└─────────────────────────────────────────────────────────────────────────────┘

Windows (PowerShell):
─────────────────────────────────────────────────────────────────────────────
iwr https://railway.app/install.ps1 | iex

Or via npm (if Node.js installed):
─────────────────────────────────────────────────────────────────────────────
npm install -g @railway/cli

Verify installation:
─────────────────────────────────────────────────────────────────────────────
railway --version


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Login to Railway                                                    │
└─────────────────────────────────────────────────────────────────────────────┘

Run in terminal:
─────────────────────────────────────────────────────────────────────────────
railway login

This will:
• Open browser for authentication
• Link your Railway account
• Store auth token locally


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Configure VS Code Settings for MCP                                  │
└─────────────────────────────────────────────────────────────────────────────┘

Path: %APPDATA%\Code\User\settings.json

Or in VS Code:
1. Press Ctrl+Shift+P
2. Type "Preferences: Open User Settings (JSON)"
3. Add this configuration:

settings.json:
─────────────────────────────────────────────────────────────────────────────
{
  // ... your existing settings ...
  
  "github.copilot.chat.mcp.servers": {
    "railway": {
      "command": "railway",
      "args": ["mcp"],
      "env": {
        "RAILWAY_TOKEN": "${env:RAILWAY_TOKEN}"
      }
    }
  }
}


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Set Railway Token (if needed)                                       │
└─────────────────────────────────────────────────────────────────────────────┘

Option A: Use railway login (recommended)
─────────────────────────────────────────────────────────────────────────────
railway login
# Token is automatically saved, no manual config needed


Option B: Manual token setup
─────────────────────────────────────────────────────────────────────────────
1. Get token from: https://railway.app/account/tokens
2. Set environment variable:

PowerShell:
[System.Environment]::SetEnvironmentVariable('RAILWAY_TOKEN', 'your-token', 'User')

Or add to Windows System Environment Variables:
• Win + R → sysdm.cpl → Advanced → Environment Variables
• User variables → New
• Variable: RAILWAY_TOKEN
• Value: your-railway-token


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Restart VS Code                                                     │
└─────────────────────────────────────────────────────────────────────────────┘

Close and reopen VS Code to load new settings.


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 6: Test Railway MCP Server                                             │
└─────────────────────────────────────────────────────────────────────────────┘

In GitHub Copilot Chat (this chat), ask:
─────────────────────────────────────────────────────────────────────────────
"Show me my Railway projects"
"Deploy this project to Railway"
"Check Railway deployment status"


═══════════════════════════════════════════════════════════════════════════════
                  OPTION 2: CLAUDE DESKTOP - Alternative
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Install Railway CLI (same as above)                                 │
└─────────────────────────────────────────────────────────────────────────────┘

iwr https://railway.app/install.ps1 | iex
railway login


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Configure Claude Desktop                                            │
└─────────────────────────────────────────────────────────────────────────────┘

Path: %APPDATA%\Claude\claude_desktop_config.json

Create or edit this file:

claude_desktop_config.json:
─────────────────────────────────────────────────────────────────────────────
{
  "mcpServers": {
    "railway": {
      "command": "railway",
      "args": ["mcp"],
      "env": {
        "RAILWAY_TOKEN": "${env:RAILWAY_TOKEN}"
      }
    }
  }
}


┌─────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Restart Claude Desktop                                              │
└─────────────────────────────────────────────────────────────────────────────┘

Close and reopen Claude Desktop application.


═══════════════════════════════════════════════════════════════════════════════
                        AVAILABLE RAILWAY MCP COMMANDS
═══════════════════════════════════════════════════════════════════════════════

Once configured, you can use these commands in chat:

┌─────────────────────────────────────────────────────────────────────────────┐
│ Deployment Commands                                                         │
└─────────────────────────────────────────────────────────────────────────────┘

"Deploy this project to Railway"
"Create a new Railway project"
"Link to existing Railway project"
"Deploy current branch"
"Trigger a new deployment"


┌─────────────────────────────────────────────────────────────────────────────┐
│ Project Management                                                          │
└─────────────────────────────────────────────────────────────────────────────┘

"Show my Railway projects"
"List all services in project"
"Get project details"
"Show deployment history"


┌─────────────────────────────────────────────────────────────────────────────┐
│ Environment & Configuration                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

"Show environment variables"
"Set environment variable DEBUG=False"
"Add DATABASE_URL variable"
"List all project variables"


┌─────────────────────────────────────────────────────────────────────────────┐
│ Logs & Monitoring                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

"Show deployment logs"
"Check build status"
"View recent logs"
"Get deployment URL"


┌─────────────────────────────────────────────────────────────────────────────┐
│ Database Commands                                                           │
└─────────────────────────────────────────────────────────────────────────────┘

"Add PostgreSQL database"
"Connect to database"
"Show database connection string"
"Run migrations on Railway"


═══════════════════════════════════════════════════════════════════════════════
                      QUICK START WORKFLOW EXAMPLE
═══════════════════════════════════════════════════════════════════════════════

After MCP setup, typical deployment conversation:

You: "Deploy this project to Railway"

AI: [Uses Railway MCP to:]
    1. Check if project exists
    2. Create new project if needed
    3. Link current directory
    4. Push code to Railway
    5. Trigger deployment
    6. Show deployment URL

You: "Add environment variable DEBUG=False"

AI: [Uses Railway MCP to:]
    1. Connect to project
    2. Set DEBUG variable
    3. Trigger redeploy
    4. Confirm change

You: "Show deployment logs"

AI: [Uses Railway MCP to:]
    1. Fetch recent logs
    2. Display in chat
    3. Highlight errors if any


═══════════════════════════════════════════════════════════════════════════════
                    TROUBLESHOOTING RAILWAY MCP SETUP
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ Issue 1: MCP Server Not Found                                               │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Error: "railway command not found"

Solution:
1. Verify Railway CLI is installed:
   railway --version

2. Check PATH includes Railway:
   PowerShell: $env:PATH -split ';' | Select-String railway

3. Reinstall if needed:
   iwr https://railway.app/install.ps1 | iex


┌─────────────────────────────────────────────────────────────────────────────┐
│ Issue 2: Authentication Failed                                              │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Error: "Not authenticated with Railway"

Solution:
1. Login again:
   railway login

2. Or set token manually:
   railway login --token your-token

3. Verify login:
   railway whoami


┌─────────────────────────────────────────────────────────────────────────────┐
│ Issue 3: MCP Server Not Loading in VS Code                                  │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Railway commands not working in Copilot Chat

Solution:
1. Check settings.json syntax (valid JSON)
2. Restart VS Code completely
3. Check Copilot logs:
   View → Output → GitHub Copilot
4. Verify GitHub Copilot Chat extension updated
5. Try: Ctrl+Shift+P → "Developer: Reload Window"


┌─────────────────────────────────────────────────────────────────────────────┐
│ Issue 4: Environment Variable Not Set                                       │
└─────────────────────────────────────────────────────────────────────────────┘

❌ RAILWAY_TOKEN not found

Solution:
1. Set system environment variable:
   [Environment]::SetEnvironmentVariable('RAILWAY_TOKEN', 'token', 'User')

2. Or remove env section from config:
   # Railway CLI uses saved login by default


┌─────────────────────────────────────────────────────────────────────────────┐
│ Issue 5: MCP Commands Timeout                                               │
└─────────────────────────────────────────────────────────────────────────────┘

❌ Commands taking too long or timing out

Solution:
1. Check internet connection
2. Verify Railway API status: https://status.railway.app
3. Try command directly in terminal:
   railway status
4. Increase timeout in settings (if available)


═══════════════════════════════════════════════════════════════════════════════
                  ALTERNATIVE: MANUAL RAILWAY CLI WORKFLOW
═══════════════════════════════════════════════════════════════════════════════

If MCP setup doesn't work, you can still use Railway CLI directly:

┌─────────────────────────────────────────────────────────────────────────────┐
│ Basic Railway CLI Commands                                                  │
└─────────────────────────────────────────────────────────────────────────────┘

# Initialize project
railway init

# Link to existing project
railway link

# Deploy
railway up

# Check status
railway status

# View logs
railway logs

# Set environment variable
railway variables set DEBUG=False

# Open in browser
railway open

# Run command in Railway environment
railway run python manage.py migrate


═══════════════════════════════════════════════════════════════════════════════
                     VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

After setup, verify these:

✅ [ ] Railway CLI installed: railway --version
✅ [ ] Logged in: railway whoami
✅ [ ] VS Code settings.json updated
✅ [ ] VS Code restarted
✅ [ ] Can run: railway status
✅ [ ] MCP commands work in Copilot Chat
✅ [ ] Can see Railway projects in chat


═══════════════════════════════════════════════════════════════════════════════
                        RECOMMENDED SETUP PATH
═══════════════════════════════════════════════════════════════════════════════

For your Tangail Doctors project, I recommend:

1. ✅ Install Railway CLI
   iwr https://railway.app/install.ps1 | iex

2. ✅ Login to Railway
   railway login

3. ✅ Add MCP config to VS Code settings
   (Copy configuration from above)

4. ✅ Restart VS Code

5. ✅ Test in this chat:
   "Show my Railway projects"

6. ✅ Deploy:
   "Deploy tangail-doctors to Railway"


═══════════════════════════════════════════════════════════════════════════════
                              USEFUL LINKS
═══════════════════════════════════════════════════════════════════════════════

• Railway MCP Docs: https://docs.railway.com/ai/mcp-server
• Railway CLI Docs: https://docs.railway.com/develop/cli
• MCP Protocol: https://modelcontextprotocol.io/
• Railway Dashboard: https://railway.app/dashboard
• Railway Status: https://status.railway.app


═══════════════════════════════════════════════════════════════════════════════

💬 NEXT STEPS:

1. বলুন: "Install Railway CLI" - আমি terminal command দিবো
2. বলুন: "Setup VS Code MCP" - আমি exact steps দিবো
3. বলুন: "Deploy now" - Setup complete হলে deploy করবো

Ready to proceed? 🚀

═══════════════════════════════════════════════════════════════════════════════

Created: February 8, 2026
For: Tangail Doctors Python Project
By: GitHub Copilot
